use java;

create table member(
mb_name varchar(20),
mb_num varchar(10),
mb_phone varchar(20),
mb_addr varchar(30)
);

desc member;

create table book(
book_num varchar(10),
book_name varchar(10),
book_author varchar(10),
book_genre varchar(10),
book_publisher varchar(10)
);